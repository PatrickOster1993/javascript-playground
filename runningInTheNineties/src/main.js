// main.js - Allgemeine Funktionen

// Gibt eine Nachricht in der Konsole aus
console.log("Willkommen auf der 90er Website 'Running in the 90s'!")

// Funktion zur Ausgabe einer Nachricht in der Konsole
function logMessage(message) {
  console.log(message) // Nachricht wird in der Konsole angezeigt
}

// Aufruf der logMessage-Funktion
logMessage("Diese Seite ist voller Retro-Spaß!") // Loggt eine Nachricht in der Konsole
